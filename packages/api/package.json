{
  "name": "@gtfs-studio/api",
  "version": "0.0.0",
  "description": "GTFS Studio backend API: spec-lock persistence + acceptance over HTTP (zero-dependency).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "gtfs-api": "./bin/gtfs-api.mjs"
  },
  "files": ["dist", "src", "bin"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ./bin/gtfs-api.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@gtfs-studio/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
