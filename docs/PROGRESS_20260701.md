# 進捗整理 2026-07-01

このメモは、GTFS Studio の現在地、今回進めた実装、公開前に残る判断、作業中に見えた設計上の知見をまとめる。

## 現在地

完成度の目安は、MVP公開準備として 90% 前後。

コアの取込、検証、v3→v4移行、出力、検収パイプラインは実装済み。Webは停留所、ダイヤ、運賃、shape、公開ゲートの主要な実務操作に到達した。APIは仕様ロック、revision保存、publish、latest配信、public URL smoke、warning承認、監査ログまでファイル永続化MVPとして動く。

ただし、実運用公開の観点では、実データ候補の許諾レビュー、Gitea Actions runnerでの実行実績、DB/RBAC方針、GTFS-RT TripUpdatesの実データ評価が残る。

## 今回進めた主な領域

### 実データ回帰と検収

- `gtfs-regression` で manifest ベースの実データ/匿名化データ回帰を実行できるようにした。
- `gtfs-anonymize` で回帰候補zipを作る導線を追加した。
- `summary.json` を A-07/A-08 の検収証跡として読み込む処理を core/Web に接続した。
- 実データ回帰の運用方針を `docs/REGRESSION.md` に整理した。

### API公開ワークフロー

- revision をファイル永続化し、`gtfs.zip` と metadata を保存できるようにした。
- ready な revision だけ publish し、`latest/gtfs.zip` として配信できるようにした。
- public URL smoke で公開URLからzipを取得し、保存済みhash、内部検証、標準validator reportを照合できるようにした。
- 静的GTFS系の書込token、監査ログ、warning承認保存を追加した。
- revision 一覧に `limit` と `status` フィルタを追加し、運用データが増えても初期表示や公開済み版確認が重くなりにくい入口にした。

### Web編集と公開ゲート

- 停留所編集、ダイヤ編集に加えて、運賃編集とshape編集を追加した。
- shape は表操作だけでなく、地図上の頂点ドラッグで編集できるようにした。
- 公開ゲートで MobilityData validator report、API仕様ロック、回帰summary、warning承認を取り込めるようにした。
- revision保存、publish、latest URL補完、public URL smoke を Web から実行できるようにした。
- API revision/latest 読込と revision 文脈同期により、読み込んだ版から編集して公開する流れが切れにくくなった。
- API revision一覧は limit/status 指定と、status/gate/acceptance/profile/日時/hash 表示に対応した。

### ドキュメント

- `docs/STATUS.md` に仕様と実装の対応を更新した。
- `docs/API_OPERATIONS.md` に API 起動、token、revision/publish/smoke、warning承認の運用手順をまとめた。
- `docs/RELEASE_CHECKLIST.md` に CLI/Web 共通の公開前チェックを分離した。
- `docs/NOTES.md` に、実装中の違和感と設計判断を継続的に記録した。

## 公開前に残るもの

- 実データ回帰に使うフィード候補の最終選定と許諾レビュー。
- 匿名化済みデータを公開判定に使う場合のレビュー済み manifest 固定。
- Gitea Actions runner 上での validator/golden/regression 実行実績。
- API永続化をファイルMVPのまま始めるか、DB/RBACを先に入れるかの判断。
- GTFS-RT TripUpdates の候補品質評価を実データで回すこと。

## 得られた知見

revision は単なる保存先ではなく、編集セッションの文脈そのものになる。`projectId` と `revisionId` を画面ごとに持つと、warning承認、publish、smoke、再編集が分断されるため、App 全体で文脈を保持する必要がある。

warning承認は操作ログではなく、公開対象 artifact に紐づく metadata として扱うべき。revision保存と承認記録が別々に残ると、後から「どのzipに対する承認だったか」が曖昧になる。

公開ゲートはブラウザ内検証だけでは完結しない。validator report、回帰summary、仕様ロック、public URL smoke は外部証跡なので、Web UI にはそれらを取り込む明示的な導線が必要になる。

CLIとWebは入口が違っても、使う証跡は同じであるべき。`report.json`、`summary.json`、仕様ロック、warning承認、smoke結果を同じ公開判定へ流せるようにすると、運用手順の説明が簡潔になる。

revision一覧は最初から増える前提で扱う方がよい。今回 `limit` と `status` フィルタを入れたことでMVPの過読込は抑えられるが、次は `cursor`、状態別件数、revision比較を足すと、長期運用の一覧画面に接続しやすい。

## 検証

- `pnpm -r build`: pass。
- `pnpm -r test`: pass。
- `packages/core`: 124 tests pass。
- `packages/api`: 43 tests pass。
- `pnpm --filter @gtfs-studio/web build`: pass。
- `pnpm --filter @gtfs-studio/api test`: pass、43 tests。
